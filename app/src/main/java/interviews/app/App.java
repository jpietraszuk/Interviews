/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package interviews.app;

import interviews.list.LinkedList;

import static interviews.utilities.StringUtils.join;
import static interviews.utilities.StringUtils.split;
import static interviews.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
